"""Bundled V18 implementation modules.

They retain their experiment-era module names internally to preserve the
validated import graph and arithmetic behavior. Public callers should use
``wal_tat.runtime`` and the ``wal-runtime`` CLI instead.
"""
